export enum State {
  TODO = "TODO",
  IN_PROGRESS = "IN_PROGRESS",
  REVIEW = "REVIEW",
  DONE = "DONE",
}

// Add other Prisma-related types/enums here if needed,
// ensuring no direct import from '@prisma/client'

